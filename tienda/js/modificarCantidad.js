function modificarCantidad(id_producto, mod) {
    console.log(id_producto, mod);
    /* fetch(URL_PATH + "/api/comprar/" + id_producto + "/" + cantidad)
        .then((res) => res.json())
        .then((res) => {
            

        }) */
}